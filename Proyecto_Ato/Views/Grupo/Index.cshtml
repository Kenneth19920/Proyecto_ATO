@model IEnumerable<Proyecto_Ato.Models.Grupo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estudiantes.IdUsuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Horario.Dia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estudiantes.IdUsuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Horario.Dia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdGrupo }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdGrupo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdGrupo })
        </td>
    </tr>
}

</table>
